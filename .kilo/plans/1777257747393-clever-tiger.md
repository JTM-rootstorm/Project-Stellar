# Phase 5E Vulkan Render Parity Breakdown

## Goal

Make `--renderer vulkan` truthful for glTF runtime rendering by replacing the current upload-record/no-op Vulkan backend with an incremental presented renderer that reaches parity with the existing OpenGL lightweight material path.

## Current State

- `VulkanGraphicsDevice` initializes a Vulkan instance, SDL surface, physical device, logical device, and graphics/present queue.
- `create_mesh`, `create_texture`, and `create_material` validate and retain backend-neutral records only.
- `begin_frame`, `draw_mesh`, and `end_frame` are explicit no-ops.
- OpenGL parity target includes `StaticVertex` attributes, base color, vertex color, UV0/UV1 routing, alpha mask/blend, double-sided culling, normal mapping, metallic/roughness factors and texture, occlusion, emissive, and uniform-array skinning capped at 96 joints.
- Default tests must remain display-free; GPU/context tests should be opt-in.

## Suggested Chunking

### Phase 5E.0: Vulkan Test Gate And Diagnostics Scaffold

Goal: add safe test/build scaffolding before rendering changes.

Tasks:

1. Add `STELLAR_ENABLE_VULKAN_CONTEXT_TESTS`, default `OFF`.
2. Add runtime env gate `STELLAR_RUN_VULKAN_CONTEXT_TESTS=1` with skip return code 77.
3. Add a minimal Vulkan initialization smoke test that creates a hidden SDL Vulkan window, initializes `VulkanGraphicsDevice`, and exits before rendering.
4. Add optional debug names/diagnostic helpers where useful, but avoid requiring validation layers in CI.
5. Keep default `ctest` display-free.

Validation:

- `cmake --build build --target stellar_graphics_backend_selection_test stellar_render_scene_inspection_test -j$(nproc)`
- `ctest --test-dir build -R '^(graphics_backend_selection|render_scene_inspection)$' --output-on-failure`
- Optional configure/build with `-DSTELLAR_ENABLE_VULKAN_CONTEXT_TESTS=ON`; smoke test should skip unless the env gate is set.

Completion notes (2026-04-27):

- Added `STELLAR_ENABLE_VULKAN_CONTEXT_TESTS` to `CMakeLists.txt`, default `OFF`, with
  `vulkan_context_smoke` registered under `SKIP_RETURN_CODE 77`.
- Added `tests/graphics/VulkanContextSmoke.cpp`, which creates a hidden
  `SDL_WINDOW_VULKAN` window, requires `STELLAR_RUN_VULKAN_CONTEXT_TESTS=1`, initializes
  `VulkanGraphicsDevice`, and exits before any frame/render work.
- Improved Vulkan initialization diagnostics by appending symbolic `VkResult` names to
  backend error messages without introducing validation-layer or debug-utils requirements.
- Validated default display-free coverage with the requested build/test commands.
- Validated opt-in scaffolding with `cmake -S . -B build-vulkan-tests
  -DSTELLAR_ENABLE_VULKAN_CONTEXT_TESTS=ON`, plus both runtime paths for
  `vulkan_context_smoke`: it skipped while `STELLAR_RUN_VULKAN_CONTEXT_TESTS` was unset and
  passed when run with `STELLAR_RUN_VULKAN_CONTEXT_TESTS=1`.

### Phase 5E.1: Swapchain And Clear-Only Presentation

Goal: make `begin_frame` and `end_frame` acquire, clear, submit, and present a frame without mesh drawing.

Tasks:

1. Query surface capabilities, formats, present modes, and select conservative defaults.
2. Create swapchain, swapchain images, image views, and per-image framebuffers or dynamic-rendering equivalents.
3. Create a render pass or dynamic rendering path with color and depth attachments.
4. Add depth image allocation and view creation.
5. Add frame acquisition, queue submission, presentation, and resize/out-of-date handling skeleton.
6. Ensure `destroy_vulkan_objects` tears down swapchain resources before device/instance resources.
7. Keep `draw_mesh` harmless while no graphics pipeline exists.

Validation:

- Existing default tests unchanged.
- Optional Vulkan context test executes a clear/present frame when a Vulkan-capable display environment is explicitly enabled.
- Manual `stellar-client --renderer vulkan` should show a black/clear window rather than a no-op window if display is available.

Completion notes (2026-04-27):

- Replaced the Vulkan frame no-op path with a presented clear-only renderer in
  `VulkanGraphicsDevice`: device initialization now creates a swapchain, swapchain image views,
  a depth image/view, a clear-only render pass, per-image framebuffers, a command pool/buffer,
  and acquire/present semaphores.
- `begin_frame` now waits for prior queue work, recreates swapchain-dependent resources when the
  SDL window size changes or when `VK_ERROR_OUT_OF_DATE_KHR` / `VK_SUBOPTIMAL_KHR` is observed,
  acquires a swapchain image, records a black color clear plus depth clear, and begins a render
  pass.
- `end_frame` now ends the render pass, submits the recorded command buffer, and presents the
  acquired swapchain image. `draw_mesh` remains harmless while no graphics pipeline exists.
- Teardown ordering was hardened so swapchain-dependent resources are destroyed before command,
  synchronization, device, surface, and instance objects.
- Updated `tests/graphics/VulkanContextSmoke.cpp` so the opt-in smoke test now initializes the
  backend and executes one clear/present frame.
- Validated default display-free coverage with the existing requested tests still passing.
- Revalidated that the opt-in Vulkan smoke test still skips cleanly when
  `STELLAR_RUN_VULKAN_CONTEXT_TESTS` is unset.
- Validated opt-in Vulkan execution with `STELLAR_RUN_VULKAN_CONTEXT_TESTS=1`, where
  `vulkan_context_smoke` completed a clear/present frame successfully.

### Phase 5E.2: Command Pools, Per-Frame Resources, And Synchronization

Goal: stabilize frame lifetime before resource uploads and draw calls grow.

Tasks:

1. Add command pool for the selected graphics queue family.
2. Add per-frame command buffers, image-available semaphores, render-finished semaphores, and in-flight fences.
3. Track a small fixed number of frames in flight.
4. Reset/re-record command buffers per frame.
5. Handle `VK_ERROR_OUT_OF_DATE_KHR` and resize by recreating swapchain-dependent resources.
6. Avoid blocking every frame except where required for safe teardown or resize.

Validation:

- Optional clear/present smoke test runs multiple frames.
- Manual window resize does not crash when using `--renderer vulkan`.

Completion notes (2026-04-27):

- Replaced the single shared Vulkan command/synchronization path with a fixed two-frame
  in-flight model in `VulkanGraphicsDevice`, matching the design document's main-thread render
  loop expectations while keeping resource ownership RAII-contained inside the device.
- Added per-frame command pools, primary command buffers, image-available semaphores,
  render-finished semaphores, and signaled in-flight fences via a private `FrameResources`
  structure.
- `begin_frame` now waits only on the current frame fence and, when needed, the fence associated
  with the acquired swapchain image, instead of stalling the whole graphics queue with
  `vkQueueWaitIdle` every frame.
- `end_frame` now submits work with the current frame fence, rotates the active frame index, and
  preserves `VK_ERROR_OUT_OF_DATE_KHR` / `VK_SUBOPTIMAL_KHR` handling for later swapchain
  recreation.
- Swapchain recreation now resets per-image fence tracking alongside swapchain-dependent Vulkan
  objects so resize/out-of-date paths remain safe as draw and upload complexity grows.
- Updated `tests/graphics/VulkanContextSmoke.cpp` so the opt-in Vulkan smoke test executes three
  clear/present frames instead of only one.
- Validated that the default display-free test subset still passes unchanged.
- Validated that `vulkan_context_smoke` still skips cleanly when
  `STELLAR_RUN_VULKAN_CONTEXT_TESTS` is unset.
- Validated that `vulkan_context_smoke` passes with `STELLAR_RUN_VULKAN_CONTEXT_TESTS=1` while
  executing multiple frames on this machine.

### Phase 5E.3: Static Mesh GPU Buffers

Goal: turn `create_mesh` from record-only into real vertex/index GPU upload for the existing `StaticVertex` layout.

Tasks:

1. Add Vulkan buffer allocation helpers for vertex, index, uniform/staging buffers.
2. Choose an initial memory strategy: direct host-visible uploads are acceptable for the first slice; staging/device-local can follow if needed.
3. Upload each mesh primitive's `StaticVertex` array and `uint32_t` index array.
4. Store per-primitive buffer handles, index count, and feature flags.
5. Destroy mesh buffers in `destroy_mesh` and backend teardown.
6. Add CPU-side tests for vertex input metadata if helpers can be isolated without a Vulkan device.

Validation:

- Existing `RenderScene` upload/inspection tests remain green.
- Optional Vulkan upload smoke test creates representative fixtures without drawing if a Vulkan device is available.

Completion notes (2026-04-27):

- Changed Vulkan mesh creation from record-only metadata retention to real per-primitive GPU
  buffer allocation and upload for the existing `StaticVertex` layout and `uint32_t` index
  streams.
- Added private Vulkan buffer helpers in `VulkanGraphicsDevice` for buffer creation, memory type
  selection reuse, host-visible/host-coherent upload via `vkMapMemory`, and buffer/memory teardown.
- Kept the initial memory strategy deliberately simple and internal: direct host-visible vertex and
  index buffers are used for this slice, deferring staging/device-local optimization until needed.
- Extended `MeshPrimitiveRecord` to retain vertex/index buffer handles, memory handles, buffer
  sizes, index counts, and existing feature flags for later pipeline binding.
- `destroy_mesh` and backend teardown now destroy mesh buffers before erasing records or destroying
  the Vulkan device.
- Added index-range validation during Vulkan mesh upload so malformed primitives fail clearly before
  GPU memory is committed for that primitive.
- Refactored `vulkan_context_smoke` to rely on the `STELLAR_ENABLE_VULKAN_CONTEXT_TESTS` CMake
  option instead of the `STELLAR_RUN_VULKAN_CONTEXT_TESTS` runtime environment gate.
- Updated the opt-in Vulkan smoke test to upload and destroy a representative two-primitive mesh
  before running multiple clear/present frames.
- Validated `render_scene_upload`, `render_scene_inspection`, and `graphics_backend_selection` in
  the default build.
- Validated the opt-in Vulkan upload/frame smoke test with a CMake configuration using
  `-DSTELLAR_ENABLE_VULKAN_CONTEXT_TESTS=ON` and no runtime environment gate.

### Phase 5E.4: Minimal Graphics Pipeline And Solid Material Draw

Goal: make Vulkan draw opaque static geometry with transforms and base color.

Tasks:

1. Add shader build strategy for Vulkan SPIR-V. Recommended: commit source GLSL/HLSL and generated SPIR-V only if the repo does not already require a shader compiler at build time; otherwise add an explicit CMake shader compile path.
2. Create descriptor layout for per-draw transforms and material factors.
3. Create pipeline layout, graphics pipeline, viewport/scissor setup, depth test, and back-face culling defaults.
4. Bind vertex/index buffers and issue indexed draws from `draw_mesh`.
5. Support fallback material color matching OpenGL when material handle is missing.
6. Initially handle only opaque, non-textured, non-skinned primitives.

Validation:

- Optional Vulkan render smoke test draws the debug cube or a generated triangle.
- Manual `stellar-client --renderer vulkan` shows a rotating cube with all faces present.

Completion notes (2026-04-27):

- Added a minimal Vulkan solid-color static draw pipeline for opaque static geometry, using
  embedded SPIR-V shaders to avoid adding a mandatory shader compiler to default build/test flows.
- Added push constants for MVP, base color, and the vertex color enable flag.
- Created the Vulkan pipeline layout and graphics pipeline with fixed viewport/scissor state,
  depth test/write enabled, and back-face culling defaults.
- `draw_mesh` now binds vertex/index buffers and records indexed draws for non-textured,
  non-skinned opaque primitives.
- Implemented missing-material fallback base color behavior for the solid draw path.
- Updated the Vulkan smoke test to create a material, draw a generated mesh, and exercise the draw
  path during opt-in context tests.
- Updated public Doxygen `@brief` documentation in `VulkanGraphicsDevice.hpp`.
- Validated default display-free coverage with `cmake --build build --target
  stellar_graphics_backend_selection_test stellar_render_scene_inspection_test
  stellar_render_scene_upload_test -j$(nproc)` and `ctest --test-dir build -R
  '^(graphics_backend_selection|render_scene_inspection|render_scene_upload)$'
  --output-on-failure`, both passing.
- Validated opt-in Vulkan coverage with `cmake --build build-vulkan-tests --target
  stellar_vulkan_context_smoke_test -j$(nproc)` and `ctest --test-dir build-vulkan-tests -R
  '^vulkan_context_smoke$' --output-on-failure`, both passing.
- Remaining limitations for later phases: textures/material shader parity, alpha/double-sided
  culling variants, and skinning.

### Phase 5E.5: Texture Images, Image Views, And Samplers

Goal: implement `create_texture` and sampler creation for backend-neutral texture state.

Tasks:

1. Map image formats and color spaces to Vulkan formats: `R8G8B8A8_SRGB`, `R8G8B8A8_UNORM`, and any required RGB handling strategy.
2. Upload images through staging buffers or host-visible images if kept deliberately simple.
3. Create image views for sampled textures.
4. Create Vulkan samplers from `SamplerAsset`: min/mag filters, mip filters, wrap modes.
5. Generate or handle mip levels consistently with OpenGL's current behavior.
6. Destroy image, view, memory, and sampler resources in `destroy_texture`.

Validation:

- Optional Vulkan texture smoke test uploads generated white/normal fixtures.
- Manual textured glTF scene renders with base color texture.

Completion notes (2026-04-27):

- Changed Vulkan texture creation from metadata-only recording to real GPU upload through a
  host-visible staging buffer, optimal-tiled `VkImage`, sampled image view, and texture-owned
  default sampler.
- Mapped backend-neutral image color spaces to `VK_FORMAT_R8G8B8A8_SRGB` and
  `VK_FORMAT_R8G8B8A8_UNORM`; RGB source images are converted internally to RGBA with alpha 255
  before upload so Vulkan uses one sampled texture format path.
- Added Vulkan mip generation with transfer blits when the selected texture format supports linear
  filtering; otherwise the backend falls back to one uploaded mip level with sampler LOD clamped to
  that level.
- Added sampler creation from `SamplerAsset` for material texture bindings, mapping min/mag
  filters, mipmap mode, and wrap modes to Vulkan sampler state while preserving the Phase 5E.4
  solid-color draw path limitation for textured materials.
- Hardened teardown so texture images, image views, memory, texture-owned samplers, and
  material-owned sampler objects are destroyed in `destroy_texture`, `destroy_material`, and backend
  teardown.
- Extended `vulkan_context_smoke` to upload generated RGB white and RGBA normal textures and to
  create a material sampler fixture without making Vulkan execution part of default CTest.
- Validated default display-free coverage with `cmake --build build --target
  stellar_graphics_backend_selection_test stellar_render_scene_inspection_test
  stellar_render_scene_upload_test -j$(nproc)` and `ctest --test-dir build -R
  '^(graphics_backend_selection|render_scene_inspection|render_scene_upload)$'
  --output-on-failure`, both passing.
- Validated opt-in Vulkan coverage with `cmake --build build-vulkan-tests --target
  stellar_vulkan_context_smoke_test -j$(nproc)` and `ctest --test-dir build-vulkan-tests -R
  '^vulkan_context_smoke$' --output-on-failure`, both passing.
- Remaining limitations for later phases: texture descriptors and shader sampling are not yet wired
  into the Vulkan draw path; textured materials continue to be skipped until Phase 5E.6.

### Phase 5E.6: Material Descriptor Routing And Lightweight Shader Parity

Goal: port OpenGL's lightweight material shader behavior into Vulkan for non-skinned static draws.

Tasks:

1. Add descriptor sets or push constants for material factors and feature flags.
2. Bind base color, normal, metallic-roughness, occlusion, and emissive textures with safe default textures when absent.
3. Implement UV0/UV1 routing per texture slot.
4. Implement vertex color multiplication.
5. Implement alpha mask discard.
6. Implement normal map shading when primitive tangents are present.
7. Implement metallic/roughness, occlusion, and emissive behavior matching the OpenGL shader, not full glTF PBR.

Validation:

- Use Phase 5D representative fixtures as the test asset source.
- Optional Vulkan render smoke tests can target deterministic basics such as clear color and simple textured/material output.
- Manual representative static glTF run with `--renderer vulkan`.

Completion notes (2026-04-27):

- Replaced the Vulkan solid-color-only shader path with embedded SPIR-V material shaders that
  sample base color, normal, metallic-roughness, occlusion, and emissive texture slots for
  non-skinned static draws without adding a default-build shader compiler dependency.
- Added a Vulkan material descriptor-set layout/pool and per-material descriptor sets with five
  combined-image-sampler bindings, using safe 1x1 default white and normal textures when material
  slots are absent or unresolved.
- Expanded draw push constants to carry material factors, alpha cutoff, packed texture feature
  flags, UV0/UV1 routing bits, vertex-color/tangent flags, and alpha-mask mode while preserving the
  128-byte push-constant size.
- `draw_mesh` now renders textured opaque and alpha-mask non-skinned static primitives, multiplies
  vertex colors when COLOR_0 is present, discards masked fragments below alpha cutoff, applies
  normal mapping only when primitive tangents are present, and matches OpenGL's lightweight
  metallic/roughness, occlusion, emissive, and directional-light behavior. Alpha-blend materials
  remain deferred to Phase 5E.7.
- Validated with `cmake --build build -j$(nproc)` and `ctest --test-dir build
  --output-on-failure`, both passing.
- Validated opt-in Vulkan coverage with `cmake --build build-vulkan-tests --target
  stellar_vulkan_context_smoke_test -j$(nproc)` and `ctest --test-dir build-vulkan-tests -R
  '^vulkan_context_smoke$' --output-on-failure`, both passing.

### Phase 5E.7: Alpha Blend, Double-Sided Culling, And Draw-State Batching

Goal: match OpenGL visible ordering and raster state for alpha and culling.

Tasks:

1. Add pipeline variants or dynamic state for opaque/mask vs blend and culling vs double-sided.
2. Enable blend with `srcAlpha`/`oneMinusSrcAlpha` for alpha blend materials.
3. Disable culling for double-sided materials and keep back-face culling otherwise.
4. Preserve `RenderScene`'s existing opaque/mask/front-to-back and blend/back-to-front submission assumptions.
5. Avoid changing backend-neutral draw ordering unless a Vulkan-specific issue proves it necessary.

Validation:

- Existing display-free ordering tests remain green.
- Optional Vulkan alpha smoke manually/visually validates alpha mask and blend fixtures.

Completion notes (2026-04-27):

- Added four Vulkan graphics pipeline variants for static material draws: opaque/mask with
  back-face culling, opaque/mask double-sided with culling disabled, alpha-blend with back-face
  culling, and alpha-blend double-sided with culling disabled.
- Enabled Vulkan alpha blending for `AlphaMode::kBlend` materials with
  `VK_BLEND_FACTOR_SRC_ALPHA` / `VK_BLEND_FACTOR_ONE_MINUS_SRC_ALPHA`, matching OpenGL's
  `glBlendFunc(GL_SRC_ALPHA, GL_ONE_MINUS_SRC_ALPHA)` behavior.
- Removed the Vulkan alpha-blend draw skip and selected the appropriate pipeline per draw command
  while preserving caller-provided `RenderScene` ordering for opaque/mask front-to-back and blend
  back-to-front submission.
- Added per-`draw_mesh` pipeline binding reuse so adjacent commands with the same alpha/culling
  state do not rebind the same draw state unnecessarily.
- Kept the backend-neutral API and draw ordering unchanged; no Phase 5E.8 skinning or Phase 5E.9
  lifetime/resize behavior was pulled into this slice.
- Validated with `cmake --build build -j$(nproc)` and `ctest --test-dir build
  --output-on-failure`, both passing.
- Validated opt-in Vulkan coverage with `cmake --build build-vulkan-tests --target
  stellar_vulkan_context_smoke_test -j$(nproc)` and `ctest --test-dir build-vulkan-tests -R
  '^vulkan_context_smoke$' --output-on-failure`, both passing.

### Phase 5E.8: Skinning Descriptor Path

Goal: render skinned glTF primitives in Vulkan with the same current limits as OpenGL.

Tasks:

1. Add joint matrix storage for draw calls, initially matching OpenGL's 96-joint cap unless a broader buffer model is intentionally chosen.
2. Add shader support for `joints0` and `weights0` attributes.
3. Bind identity/no-skin path for static primitives and joint matrices for skinned primitives.
4. Validate over-limit skin palettes fail or fall back clearly rather than reading invalid descriptor data.
5. Keep larger skin palette expansion for Phase 5F unless explicitly pulled forward.

Validation:

- Existing animation/runtime and `RenderScene` skin pose forwarding tests remain green.
- Optional Vulkan skinned fixture smoke test.
- Manual animated/skinned glTF run with `--renderer vulkan`.

Completion notes (2026-04-27):

- Added Vulkan skinned draw support for primitives with `JOINTS_0` / `WEIGHTS_0`, matching
  OpenGL's current 96-joint cap with Vulkan-private `kMaxSkinJoints = 96`.
- Added set `1` vertex-stage uniform-buffer descriptors backed by per-frame host-visible,
  host-coherent draw-uniform buffers. Each frame owns 256 descriptor-backed slots and resets an
  upload cursor at `begin_frame`, so multiple static or skinned draws can safely upload per-draw
  transform/skin data without push constants for joint matrices.
- Chose a single per-draw vertex uniform block containing `mvp`, `world`, padded normal matrix,
  `has_skinning`, `joint_count`, and a 96-`mat4` skin palette; material factors and material flags
  remain in the existing 128-byte push-constant block for fragment/material parity.
- Extended the Vulkan pipeline layout with material set `0` plus transform/skin set `1`, and added
  vertex attributes location `6` (`VK_FORMAT_R16G16B16A16_UINT` joints0) and location `7`
  (`VK_FORMAT_R32G32B32A32_SFLOAT` weights0).
- Regenerated the embedded Vulkan material vertex SPIR-V so skinned positions, normals, and tangents
  follow the OpenGL lightweight skinning math while preserving the existing fragment/material shader
  behavior.
- Removed the previous Vulkan skinned-primitive skip. Static primitives and skinned primitives with
  empty draw palettes use `has_skinning = false`; valid skinned draws upload and bind their palette;
  over-limit palettes or palettes smaller than referenced joint indices are skipped with clear
  Vulkan diagnostics.
- Added upload-time validation for skinned Vulkan primitives: joint indices must be below 96 and
  weights must be finite. Draw-time validation rejects palettes over 96 matrices and undersized
  palettes for the primitive's recorded max joint index.
- Extended `vulkan_context_smoke` with a generated skinned triangle using joints0/weights0, a
  non-identity one-matrix palette draw, and a 97-matrix over-limit safety draw while keeping Vulkan
  context coverage opt-in via `STELLAR_ENABLE_VULKAN_CONTEXT_TESTS`.
- Validated default display-free coverage with `cmake --build build -j$(nproc)` and
  `ctest --test-dir build --output-on-failure`; both passed. Also validated the required subset with
  `cmake --build build --target stellar_graphics_backend_selection_test
  stellar_render_scene_inspection_test stellar_render_scene_upload_test
  stellar_animation_runtime_test -j$(nproc)` and `ctest --test-dir build -R
  '^(graphics_backend_selection|render_scene_inspection|render_scene_upload|animation_runtime)$'
  --output-on-failure`; both passed.
- Validated opt-in Vulkan coverage with `cmake -S . -B build-vulkan-tests
  -DSTELLAR_ENABLE_VULKAN_CONTEXT_TESTS=ON`, `cmake --build build-vulkan-tests --target
  stellar_vulkan_context_smoke_test -j$(nproc)`, and `ctest --test-dir build-vulkan-tests -R
  '^vulkan_context_smoke$' --output-on-failure`; all passed.
- Remaining limitations deferred: larger skin palettes / storage-buffer or multi-palette model remain
  Phase 5F work; resize/lifetime hardening remains Phase 5E.9.

### Phase 5E.9: Resize, Recreate, And Resource Lifetime Hardening

Goal: make the Vulkan renderer robust enough for normal client use.

Tasks:

1. Recreate swapchain-dependent resources on window resize, suboptimal swapchain, or out-of-date presentation.
2. Ensure in-flight resource lifetime is safe when meshes/textures/materials are destroyed.
3. Wait for device idle only on teardown or unavoidable resize paths.
4. Audit all Vulkan calls for clear error reporting through `std::expected` during initialization/upload paths.
5. Keep frame functions `noexcept`; log or safely no-op after unrecoverable frame errors.

Validation:

- Manual resize test with static cube and representative glTF.
- Optional Vulkan smoke test that recreates swapchain if feasible in hidden-window environment.

Completion notes (2026-04-27):

- Hardened Vulkan swapchain recreation on resize, minimized/zero-size windows, suboptimal
  acquire/present, and out-of-date acquire/present paths.
- Ensured frame state is reset safely after failed acquire, command recording, submit, present, and
  recreate paths while keeping `begin_frame`, `draw_mesh`, and `end_frame` `noexcept`.
- Implemented a deferred deletion queue for in-flight mesh, texture, material, descriptor, and
  sampler lifetime safety.
- Deferred deletion records retire after conservative per-frame fence serials complete; completed
  serials are advanced when frame fences are waited before reuse, eligible records are drained at
  frame begin, and all pending records are force-drained during teardown after `vkDeviceWaitIdle`.
- Rewrote material descriptors when dependent textures are destroyed, rebinding affected slots to
  default white/normal texture views with existing material samplers where available and falling
  back to safe material invalidation if defaults are unavailable.
- Audited Vulkan initialization, upload, recreate, and frame error paths so checked `VkResult`
  failures report operation-specific `std::expected` errors or frame-path log messages.
- Hardened teardown ordering for partial initialization, failed recreation, normal shutdown, and
  pending deferred deletions.
- Extended the opt-in Vulkan smoke test to cover resize/recreate and resource-destroy-after-frame
  behavior where supported by the hidden SDL Vulkan window environment.
- Validated default display-free coverage with: `cmake --build build -j$(nproc)` and
  `ctest --test-dir build --output-on-failure`; both passed.
- Validated opt-in Vulkan coverage with: `cmake -S . -B build-vulkan-tests
  -DSTELLAR_ENABLE_VULKAN_CONTEXT_TESTS=ON`, `cmake --build build-vulkan-tests --target
  stellar_vulkan_context_smoke_test -j$(nproc)`, and `ctest --test-dir build-vulkan-tests -R
  '^vulkan_context_smoke$' --output-on-failure`; all passed.
- Manual validation: ran `timeout 5s ./build/stellar-client --renderer vulkan`; the client ran until
  timeout without crashing and logged a recoverable `VK_SUBOPTIMAL_KHR` present during normal
  swapchain recovery. Static/glTF/skinned visual resize validation was not performed beyond the
  opt-in smoke coverage in this environment.

### Phase 5E.10: Vulkan Parity Test Matrix And Completion Gate

Goal: declare Phase 5E complete only when core behavior is exercised and documented.

Tasks:

1. Keep default CTest display-free.
2. Add opt-in Vulkan tests for initialization, clear/present, static triangle/cube, textured material, alpha mask/blend, and skinned draw where feasible.
3. Reuse generated representative fixtures from Phase 5D to avoid external asset dependencies.
4. Add a manual validation checklist for static and skinned glTF assets.
5. Update `.kilo/plans/glTF-vulkan-parity.md` only after completed implementation slices, not before.

Validation:

- Default build and CTest pass without GPU/display assumptions.
- Optional Vulkan context tests pass on a Vulkan-capable system when explicitly enabled.
- Manual `stellar-client --renderer vulkan` renders representative static and skinned glTF scenes.

Completion notes (2026-04-27):

- Audited Phase 5E Vulkan coverage and confirmed the default test suite remains display-free:
  `render_scene_upload`, `render_scene_inspection`, `graphics_backend_selection`,
  `animation_runtime`, `client_asset_validation_smoke`, `client_cli_asset_validation`, and the
  currently enabled `gltf_importer_regression` do not create a Vulkan window or require a display.
- Confirmed `STELLAR_ENABLE_VULKAN_CONTEXT_TESTS` remains default `OFF` in `CMakeLists.txt`, and
  `vulkan_context_smoke` is still built/registered only inside the opt-in CMake block.
- Expanded `tests/graphics/VulkanContextSmoke.cpp` opt-in coverage for hidden SDL Vulkan
  initialization, multiple clear/present frames, generated static triangle draw, base-color texture,
  vertex color, normal texture with tangents, metallic-roughness texture/factors, occlusion
  texture/strength, emissive texture/factor, missing optional texture fallback, alpha mask, alpha
  blend, back-face-culled and double-sided pipeline variants, valid skinned draws within the
  96-joint cap, empty/static skin palette behavior, undersized-palette skip, over-limit 97-joint
  skip, hidden-window resize/recreate where SDL reports the size change, and destroy-after-submit
  lifetime/deferred-deletion drain frames.
- Reused generated deterministic in-test fixtures rather than external assets for automated Vulkan
  smoke coverage; no mandatory GPU readback, validation-layer, shader compiler, asset-pipeline, or
  public graphics API dependency was added.
- Added the manual validation checklist to `Plans/Phase5E10.md` with `./build/stellar-client
  --renderer vulkan`, `./build/stellar-client --renderer opengl`, representative asset
  placeholders, resize/shutdown checks, and OpenGL comparison. Exact representative glTF paths were
  not available in the repo during this slice.
- Validated default build/tests with:
  - `cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug`
  - `cmake --build build -j$(nproc)`
  - `ctest --test-dir build --output-on-failure`
  - Result: passed, 7/7 tests.
- Validated opt-in Vulkan build/tests with:
  - `cmake -S . -B build-vulkan-tests -DCMAKE_BUILD_TYPE=Debug -DSTELLAR_ENABLE_VULKAN_CONTEXT_TESTS=ON`
  - `cmake --build build-vulkan-tests --target stellar_vulkan_context_smoke_test -j$(nproc)`
  - `ctest --test-dir build-vulkan-tests -R '^vulkan_context_smoke$' --output-on-failure`
  - Result: passed, 1/1 Vulkan smoke test.
- Manual validation:
  - `timeout 5s ./build/stellar-client --renderer vulkan`
  - Result: ran until timeout without crashing; logged recoverable `VK_SUBOPTIMAL_KHR` present
    during swapchain recovery. Full representative static/skinned glTF visual comparison was not
    performed because exact asset paths were unavailable.
- Phase 5E completion status: complete.
- Remaining follow-up: Phase 5F should address larger skin palettes/storage-buffer expansion,
  broader manual asset-library visual comparisons, and any future optional GPU-readback or
  validation-layer harness if needed.

## Recommended Implementation Order

1. 5E.0 test gate and diagnostics scaffold.
2. 5E.1 clear-only swapchain presentation.
3. 5E.2 command buffers and synchronization.
4. 5E.3 mesh GPU buffers.
5. 5E.4 minimal solid-color static draw.
6. 5E.5 texture upload and samplers.
7. 5E.6 material shader parity for static meshes.
8. 5E.7 alpha and culling parity.
9. 5E.8 skinning.
10. 5E.9 resize/lifetime hardening.
11. 5E.10 parity completion gate.

## Suggested Agent Routing

- `@miyamoto`: all Vulkan rendering, shaders, swapchain, pipelines, descriptors, and GPU tests.
- `@carmack`: only if backend-neutral `GraphicsDevice` lifetime or resource ownership API changes become necessary.
- `@kojima`: only if representative gameplay/glTF scene fixtures need design input.

## Key Decisions To Keep Explicit

- Shader toolchain: choose between checked-in SPIR-V or a CMake-time compiler dependency before Phase 5E.4.
- Memory allocation: start with simple internal Vulkan allocation; introduce VMA or a larger allocator only if manual allocation becomes a blocker.
- Vulkan API style: choose render pass/framebuffer vs dynamic rendering based on target Vulkan 1.2 constraints and driver support.
- Skinning limit: match OpenGL's current 96-joint limit for Phase 5E; defer larger palettes to Phase 5F unless required.
- Testing: never make GPU/display Vulkan tests part of default CTest.
